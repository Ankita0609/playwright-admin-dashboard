pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                echo 'Build step goes here'
            }
        }

        stage('Test') {
            steps {
                echo 'Test step goes here'
            }
        }
    }
}
